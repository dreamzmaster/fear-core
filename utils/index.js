'use strict';

/**
 * @module utils
 */
module.exports = {

    /**
     * get config settings
     * @see module:utils/config
     */
    config: require('./config'),

    /**
     * file system utils
     * @see module:utils/fs
     */
    fs: require('./fs')
};
