'use strict';

/**
 * load paths
 */
var paths = require('./defaults/paths');
console.log('paths', paths);

/**
 * create folder structure
 * -app
 * -config
 * -tasks
 * -test
 */

/**
 * copy paths config
 */

/**
 * copy serve task
 */
