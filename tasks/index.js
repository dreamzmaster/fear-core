'use strict';

/**
 * @module tasks
 */
module.exports = {

    /**
     * mock data tasks
     * @see module:tasks/mocks
     */
    mocks : {
        copy: require('./mock/copy'),
        copyInline: require('./mock/copy-inline')
    }
};